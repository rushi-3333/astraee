# ASTRAE — Core dependencies (install with: pip install -r requirements.txt)
Django>=6.1,<7
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
catboost>=1.2
joblib>=1.3

# Optional — ML search & recommendations (graceful fallback if missing)
# chromadb
# sentence-transformers
# tensorflow
