# =============================================================================
# ASTRAE — Python Dependencies
# Install: pip install -r requirements.txt
# =============================================================================

# --- Required (core application) ---
Django>=6.1,<7
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
catboost>=1.2
joblib>=1.3

# --- Optional (ML vector search — app falls back to mock providers if omitted) ---
# chromadb>=0.4
# sentence-transformers>=2.2
